/*
Auteurs : Mathieu Zeman
Nom du fichier : main.c

Exercice :

Objectif:
EN utilisant les listes simplements chainées, implémenter les fonctions suivante :

void iserer(list *, int) prend deux entrées : une liste d'entiers simplement chainée et un nouvel entier. L'objectif de cette fonction est d'ajouter ce nombre au début de la liste chainée

int supprimer (list *) prend une entrée : une liste d'entiers simplement chainée. L'objectif de cette fonctions est de supprimée le premier éléments et de le r'envoyer.


*/
#include <stdio.h>

void main()
{
    struct LinkedList* head;

    return;
}








}